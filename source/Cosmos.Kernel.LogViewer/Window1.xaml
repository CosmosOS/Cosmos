<Window x:Class="Cosmos.Kernel.LogViewer.Window1"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:local="clr-namespace:Cosmos.Kernel.LogViewer.MessageTemplates"
    Title="Window1" Height="362" Width="559">
	<Window.Resources>
		<local:MessageTemplateSelector x:Key="OverviewTemplateSelector"/>
	</Window.Resources>
	<DockPanel LastChildFill="True">
		<Menu DockPanel.Dock="Top">
			<MenuItem Header="_File"/>
		</Menu>
		<TabControl>
			<TabItem Header="Overview">
			</TabItem>
			<TabItem Selector.IsSelected="True" Header="Everything">
				<ListView ItemsSource="{Binding Source={StaticResource LogFile}}" ItemTemplateSelector="{StaticResource OverviewTemplateSelector}">
				</ListView>
			</TabItem>
		</TabControl>
	</DockPanel>
</Window>
