<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>enable</Nullable>
    <AllowUnsafeBlocks>true</AllowUnsafeBlocks>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="../Cosmos.TestRunner.Protocol/Cosmos.TestRunner.Protocol.csproj" />
    <!-- Reuse the QEMU launcher and tool resolver from cosmos run so tests and CLI agree on argv. -->
    <ProjectReference Include="../../src/Cosmos.Tools/Cosmos.Tools.csproj" />
  </ItemGroup>

  <ItemGroup>
    <!-- The host tests pin how a guest's host requests parse and which QMP events a pointer request sends. -->
    <InternalsVisibleTo Include="Cosmos.Tests.Patcher" />
  </ItemGroup>

</Project>
